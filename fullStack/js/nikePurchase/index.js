let purchaseStatus = document.getElementById("status");

function purchase(){
    purchaseStatus.innerText ="Status: ";
    console.log(purchaseStatus);
    purchaseStatus.innerText += " Purchase Error";
}